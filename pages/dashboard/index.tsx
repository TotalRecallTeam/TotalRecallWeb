import { Layout } from "@/components/Layout";
import { TokenDataTabs } from "@/components/Tabs";

const index = () => {
  return (
    <Layout>
      <TokenDataTabs />
    </Layout>
  );
};

export default index;
